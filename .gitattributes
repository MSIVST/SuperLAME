# Normalize line endings. Text files use LF in the repo; shell scripts MUST stay
# LF (they run under bash). The vendored dr_flac header and the icon are left
# untouched.
* text=auto eol=lf
*.ps1   text eol=crlf
*.ico   binary
dr_flac/dr_flac.h -text
